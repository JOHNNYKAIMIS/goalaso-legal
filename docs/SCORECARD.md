# Goalaso Legal — Βαθμολογία ποιότητας (SOURCE OF TRUTH)

> Πρώτη μέτρηση: 2026-08-21, HEAD `30421cc`. Επαλήθευση 2026-08-22 σε clone, ίδιο HEAD — όλες οι
> γραμμές ξαναμετρήθηκαν· ό,τι διαφέρει από την πρώτη μέτρηση φέρει ημερομηνία.
> Γύρος 1: 2026-08-22 — κλάση G04 απέκτησε φύλακα με απόδειξη (branch `g04-quality-workflow`).
> Γύρος 2: 2026-08-22 — κλάσεις G02 + G03 απέκτησαν φύλακες με απόδειξη (branch `g02-g03-guards`)·
> το σημερινό χρέος κλειδώθηκε στο [docs/known-debt.json](known-debt.json) με λόγο και λήξη 2026-09-21.
> Γύρος 3: 2026-08-22 — νέα κλάση G05 (το live αποκλίνει από το main) με φύλακα και απόδειξη (branch `g05-deploy-smoke`).
> Γύρος 4: 2026-08-22 — G01 μετρήθηκε στον κώδικα της εφαρμογής (`goalaso` @ `ad56785`): το περιστατικό ΞΑΝΑΣΥΝΕΒΗ
> (όνομα/επώνυμο από 18/8) + 3 κενά· κατάλογος `docs/claims.json` + φύλακας `claims:check` με απόδειξη (branch `g01-claims`).
> Κάθε γραμμή έχει anchor που ΤΡΕΞΕ — όχι εντύπωση. Βαθμός ανεβαίνει ΜΟΝΟ όταν ξαναμετρηθεί.

## Τι είναι αυτό το repo (χαρτογράφηση 2026-08-21)

Τρεις στατικές σελίδες HTML — `index`, `privacy`, `terms` (`wc -l *.html` → μετρήθηκε 192 γραμμές
συνολικά στις 2026-08-21) — οι νομικές σελίδες της εφαρμογής Goalaso (οργάνωση ερασιτεχνικού
futsal). Δίγλωσσες (ελληνικά + αγγλικά στο ίδιο αρχείο). Μέχρι τον γύρο 1: κανένα build, κανένα
test, κανένα CI (`ls -A .github 2>/dev/null` → τίποτα στις 2026-08-21). Ό,τι γίνεται push βγαίνει
όπως είναι. Από τον γύρο 1 (2026-08-22): `.github/workflows/quality.yml` σε κάθε push + PR,
`npm run check` τοπικά, χωρίς εξαρτήσεις (`cat package.json` → `scripts` μόνο).

**Πώς βγαίνει (μετρήθηκε 2026-08-22):** GitHub Pages με source `main` `/`, legacy build
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/pages --jq '.source,.build_type'`). Μέχρι τον γύρο 1 το
ΜΟΝΟ που έτρεχε σε push ήταν το dynamic `pages-build-deployment`
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/actions/workflows --jq '.workflows[].path'` → μόνο
`dynamic/pages/pages-build-deployment`) — deploy, όχι έλεγχος. Δηλαδή **push στο main =
δημοσίευση μέσα σε δευτερόλεπτα** (push 16:23:26Z → run 16:23:31Z), χωρίς branch protection
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/branches/main/protection` → 404). Το
`goalaso.net/privacy.html` ΔΕΝ είναι αυτό το repo: επιστρέφει το SPA της εφαρμογής
(`curl -s https://goalaso.net/privacy.html | grep -o '<title>[^<]*'` → «Goalaso — Όλα για την
μπάλα.»). Ο σύνδεσμος που βλέπουν χρήστες και stores είναι το github.io (app repo:
`grep -rn goalaso-legal apps/mobile/src/utils/support.ts docs/marketing/play-console-paste-sheet.md`).

**Ποιος τις διαβάζει:** οι χρήστες της εφαρμογής (link από τα stores/app) και —το σημαντικότερο—
όποιος ασκήσει δικαίωμα GDPR ή υποβάλει καταγγελία στην Επίτροπο Κύπρου.

**Το χειρότερο πιθανό σφάλμα:** η πολιτική να υπόσχεται κάτι που η εφαρμογή δεν κάνει (ή το
αντίστροφο). Δεν είναι υπόθεση — συνέβη: το ιστορικό (`git log --oneline`) δείχνει ότι η
πολιτική περιέγραφε διαγραφή «μόνο μέσω email» ενώ η εφαρμογή απέκτησε in-app διαγραφή, και
διορθώθηκε με το χέρι όταν το πρόσεξε κάποιος (commit `30421cc`, 2026-08-03).

**Τι το εμποδίζει σήμερα:** από τον γύρο 1 ο φύλακας G04 (δομή HTML, εσωτερικά links,
κωδικοποίηση) και από τον γύρο 2 οι φύλακες G02 (δομή/ημερομηνία/γλώσσα EL = EN) και G03
(ημερομηνία σελίδας = τελευταίο ουσιώδες commit), όλοι με απόδειξη, σε κάθε push/PR. Από τον
γύρο 3 ο φύλακας G05: το live σερβίρει byte-προς-byte το `origin/main`, χωρίς CDN cache, σε κάθε
push/PR και καθημερινά (`deploy-smoke.yml`). Για το χειρότερο σφάλμα (G01), από τον γύρο 4: ο
κατάλογος [claims.json](claims.json) δένει κάθε ισχυρισμό με το κείμενο και με μέτρηση στον κώδικα
της εφαρμογής· ο φύλακας `claims:check` τρέχει εδώ (anchors, χρέος με λήξη, μέτρηση ≤ 60 ημερών) και
με `--app` στον φάκελο της εφαρμογής — αλλά ΟΧΙ ακόμα στο CI της εφαρμογής (απόφαση ιδιοκτήτη,
[app-release-checklist.md](app-release-checklist.md)). `node scripts/catalogue-status.mjs` →
[STATUS.md](failure-catalogue/STATUS.md). Το σημερινό χρέος (λάθος ημερομηνία στο privacy,
`lang="en"` σε 0/2 σελίδες, 2 ψευδείς + 2 ελλιπείς δηλώσεις της πολιτικής) ΠΑΡΑΜΕΝΕΙ στις σελίδες —
κλειδωμένο, με λήξη 2026-09-21, όχι διορθωμένο: η διόρθωση αλλάζει HTML = παραγωγή = απόφαση ιδιοκτήτη.

## Η κλίμακα

| | |
|---|---|
| ≤4 | Το πρόβλημα υπάρχει και κανείς δεν το βλέπει |
| 5-6 | Το ξέρουμε, δεν το εμποδίζει τίποτα |
| 7-8 | Υπάρχει μηχανισμός, αλλά δεν έχει αποδειχθεί ότι πιάνει |
| 9 | Μηχανισμός που έχει αποδειχθεί ότι πέφτει όταν σπάσει αυτό που φυλάει |
| 10 | Το χρέος δεν υπάρχει πια |

## Α. Ακρίβεια περιεχομένου — (6+7+5)/3 = 6,0/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Συμφωνία πολιτικής ↔ εφαρμογής | **6** | 8 | Οι ισχυρισμοί ζούσαν ΜΟΝΟ σε πεζογραφία· η απόκλιση ΕΧΕΙ συμβεί (commit `30421cc`). **Γύρος 4 (2026-08-22) — μετρήθηκε στον κώδικα της εφαρμογής (`goalaso` @ `ad56785`):** το περιστατικό ΞΑΝΑΣΥΝΕΒΗ 15 μέρες μετά τη διόρθωση — migration `20260818000032_first_last_name.sql` + πεδίο «Όνομα» σε 9 αρχεία, ενώ η πολιτική λέει μόνο «όνομα εμφάνισης». Και 3 ΚΕΝΑ: αναφορές σφαλμάτων (`crash_reports` με `auth_id`/`route`), λίστα αναμονής με email καλεσμένων (`POST /waitlist` → `waitlist_emails`), επεξεργαστές Render (API, Frankfurt) / Expo (OTA) που δεν κατονομάζονται. Ό,τι η πολιτική λέει αρνητικά ισχύει: 0 SDK κάμερας/GPS/πληρωμών/διαφημίσεων, `avatar_url` ανενεργό. Κατάλογος [claims.json](claims.json): 18 ισχυρισμοί, anchors EL+EN 16/16, 12 ΕΠΙΒΕΒΑΙΩΜΕΝΟΙ, 2 ΔΙΑΨΕΥΣΜΕΝΟΙ, 2 ΚΕΝΑ, 2 χειροκίνητοι· φύλακας `claims:check` (anchors ↔ κείμενο, κάθε ψευδής/ελλιπής δήλωση με χρέος + λήξη, μέτρηση ≤ 60 ημερών αλλιώς κόκκινο) και `--app` ξαναμέτρηση (16/16 συμφωνούν)· απόδειξη 13/13 κόκκινες, 3/3 αθώες, fail-closed 4/4. 5 → **6**: ξέρουμε ακριβώς, ανά ισχυρισμό, με εντολή — αλλά ο μηχανισμός που πιάνει την ΑΛΛΑΓΗ της εφαρμογής ζει μόνο ως `--app` (τοπικά / release checklist), όχι στο CI της εφαρμογής. **Για 7:** το προτεινόμενο workflow ([app-release-checklist.md](app-release-checklist.md)) τρέχει πράσινο στο repo της εφαρμογής. **Για 8:** αποδεδειγμένο ότι κοκκινίζει σε PR της εφαρμογής + η πολιτική ενημερωμένη (4 εγγραφές χρέους, λήγουν 2026-09-21 — ουσιώδης αλλαγή → νέα ημερομηνία) ([G01](failure-catalogue/G01-policy-lags-app.md)) | `node scripts/claims-check.mjs --app ../goalaso` |
| Ημερομηνία «Τελευταία ενημέρωση» αληθής | **7** | 9 | Μετρήθηκε 2026-08-21: το privacy.html γράφει «4 Αυγούστου 2026», αλλά το commit που το έγραψε έγινε **3 Αυγούστου 19:23 +0300** (`30421cc`· τα 3 προηγούμενα commits έγραφαν «3 Αυγούστου»). Ήταν 4 → 5 με την καταγραφή. **Γύρος 2 (2026-08-22):** φύλακας `date:check` — ημερομηνία σελίδας = `%cs` του τελευταίου ουσιώδους commit, `[no-date]` αγνοείται· απόδειξη `date:prove` 11/11 μεταλλάξεις κόκκινες, 3/3 αθώες πράσινες, fail-closed 3/3 (όχι git, ρηχό clone, 0 σελίδες). Αναπαραγωγή: χωρίς known-debt → ΚΟΚΚΙΝΟ στον σημερινό κώδικα (`privacy.html:20: 2026-08-04 ≠ 30421cc 2026-08-03`)· terms 1/1 σωστό. 5 → **7**: μηχανισμός αποδεδειγμένος, ΑΛΛΑ το λάθος της γραμμής ΠΑΡΑΜΕΝΕΙ στη σελίδα — κλειδωμένο στο known-debt.json (λήγει 2026-09-21), όχι διορθωμένο. **Για 9:** ο ιδιοκτήτης διορθώνει (commit `[no-date]`, «3 Αυγούστου 2026» / «August 3, 2026»), αφαιρεί την εγγραφή χρέους, merge στο main, ξαναμέτρηση ([G03](failure-catalogue/G03-hand-maintained-date.md)) | `node scripts/date-check.mjs` · `git log --format='%h %cs' -- privacy.html` |
| Στοιχεία επικοινωνίας ενεργά | **5** | 8 | Το κείμενο δηλώνει το gmail «προσωρινό — θα αλλάξει σε privacy@goalaso.net μόλις ενεργοποιηθεί το routing» (μετρήθηκε: `grep -n "privacy@goalaso.net" privacy.html` → 1 εμφάνιση, μόνο στα ελληνικά). Εκκρεμής απόφαση ΧΩΡΙΣ ημερομηνία λήξης. **ΔΕΝ ΜΕΤΡΗΘΗΚΕ:** αν το mailbox απαντά — θέλει δοκιμαστικό email από άνθρωπο. **Για 8:** ή ενεργοποίηση του routing ή αφαίρεση της υπόσχεσης | `privacy.html:26` |

## Β. Δίγλωσση συνέπεια — 7/1 = 7,0/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Ισοδυναμία EL ↔ EN | **7** | 9 | Δομικά ΟΚ (2026-08-21: terms 8/8 h2, privacy 4/4 bullets, ημερομηνίες ίδιες) ΑΛΛΑ απόκλιση περιεχομένου: η σημείωση «προσωρινό email» υπάρχει ΜΟΝΟ στα ελληνικά. Επιπλέον (2026-08-22): `lang="en"` σε **0/2 δίγλωσσες** σελίδες (το index.html είναι μονόγλωσσο — το «0/3» ήταν grep), το αγγλικό μισό δηλώνεται ελληνικό. **Γύρος 2 (2026-08-22):** φύλακας `bilingual:check` — h2/ul/li/a/p EL = EN (με εξαίρεση το EL-only `lang-note`), ημερομηνία EL = EN, αγγλικό τμήμα σε `lang="en"`· απόδειξη `bilingual:prove` 12/12 κόκκινες, 3/3 αθώες πράσινες, fail-closed 2/2. Μετρήθηκε: h2 7=7/8=8 · ul 2=2/0=0 · li 8=8/0=0 · a 3=3/1=1 · p 7=7/10=10, ημερομηνίες 2/2, lang="en" 0/2 → γνωστό χρέος (λήγει 2026-09-21). Αναπαραγωγή: χωρίς known-debt → ΚΟΚΚΙΝΟ (2 σφάλματα). 5 → **7**: μηχανισμός αποδεδειγμένος, το χρέος ζει· η σημασιολογική απόκλιση («προσωρινό») ΔΕΝ πιάνεται από φύλακα — μόνο άνθρωπος. **Για 9:** `<section lang="en">` και στις 2 σελίδες (commit `[no-date]`), αφαίρεση εγγραφών χρέους, η EN σημείωση «προσωρινό» (ανθρώπινη απόφαση), merge + ξαναμέτρηση ([G02](failure-catalogue/G02-bilingual-divergence.md)) | `node scripts/bilingual-check.mjs` · `grep -c 'lang="en"' privacy.html terms.html` |

## Γ. Μηχανική παράδοσης — (7+8)/2 = 7,5/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Έλεγχοι σε κάθε αλλαγή | **7** | 9 | Ήταν 2 (2026-08-21: κανένα workflow στο repo, κανένα script· 2026-08-22: το push του `cc73d1f` πυροδότησε 0 runs — `gh api ".../actions/runs?branch=quality-baseline" --jq .total_count`). **Γύρος 1 (2026-08-22):** `quality.yml` σε push + PR με φύλακα `html:check` για (α) δομή HTML, (β) εσωτερικά links, (γ) κωδικοποίηση, απόδειξη `html:prove`, και `catalogue:check` για το παραγόμενο STATUS.md. Μετρήθηκε τοπικά ΚΑΙ στο CI (run `32567108802` @ `f55353f` → success, 6/6 βήματα, ίδιες γραμμές λογοδοσίας στο log — `gh run view 32567108802 --log \| grep '✓'`): ✓ 3 αρχεία, 2/2 εσωτερικά links, 2 εξωτερικά (ΔΕΝ ελέγχθηκαν), 0 mojibake· prove 13/13 μεταλλάξεις κόκκινες, 5/5 αθώες πράσινες, fail-closed 2/2. Η απόδειξη έπιασε false positive της 1ης έκδοσης ΠΡΙΝ μπει στο CI ([G04](failure-catalogue/G04-nothing-runs-on-push.md)). **Γύρος 2:** (δ) ΕΓΙΝΕ — οι φύλακες G02 + G03 με αποδείξεις μέσα στο ίδιο workflow (`fetch-depth: 0`). 2 → **7**: μηχανισμός υπάρχει και αποδείχθηκε, ΑΛΛΑ το main δεν το έχει — η ετυμηγορία μετρά στο SHA του trunk. **Για 9:** merge στο main και πράσινο run εκεί (ξαναμέτρηση) | `ls .github/workflows` · `npm run check` · `gh run list --workflow quality --limit 3` |
| Το ζωντανό deploy σερβίρει το HEAD | **8** | 9 | Μετρήθηκε 2026-08-22 (στις 2026-08-21 το egress ήταν μπλοκαρισμένο): 3/3 αρχεία byte-identical με το HEAD· run `pages build and deployment` @ `30421cc` success → 7 (μηχανισμός, καμία επαλήθευση). **Παγίδες (μετρημένες):** `gh api .../pages/builds/latest --jq .commit` δείχνει `9bfa4b6` (το ΠΡΟΗΓΟΥΜΕΝΟ commit) ενώ σερβίρεται το `30421cc`· `Cache-Control: max-age=600` — το CDN σερβίρει έως 10 λεπτά παλιό περιεχόμενο, έλεγχος χωρίς cache-buster θα έλεγε «όλα καλά» σε παλιό. **Γύρος 3 (2026-08-22):** φύλακας `deploy:check` — κάθε `*.html` του `origin/main` (όχι του checkout — σε PR το live είναι ακόμα το main) κατεβαίνει με `?nocache=` + `Cache-Control: no-cache` και συγκρίνεται byte-προς-byte, με αναμονή για το deploy (600 s σε push στο main)· μετρήθηκε ✓ 3/3 (603 / 8104 / 5941 B) σε 0,2 s. Απόδειξη `deploy:prove` σε τοπικό HTTP server: 4/4 κόκκινες (1 byte διαφορά, 404, 500, παλιό πέρα από το παράθυρο), 4/4 αθώες (ίδια bytes, παλιό που φρεσκάρει μέσα στο παράθυρο, άλλα headers, cache-buster σε κάθε αίτημα), fail-closed 3/3 (ref λείπει, 0 αρχεία, server κλειστός). Η απόδειξη έπιασε crash του φύλακα στα Windows (`process.exit` μετά από `fetch`) πριν το CI. Τρέχει σε κάθε push/PR (`quality.yml`) και καθημερινά (`deploy-smoke.yml`, drift). 7 → **8**: αποδεδειγμένος μηχανισμός, αλλά ΔΕΝ έχει τρέξει ακόμα μετά από πραγματικό deploy (το main δεν το έχει). **Για 9:** merge, πρώτος πράσινος run στο main μετά από push (με την αναμονή), πρώτος scheduled run ([G05](failure-catalogue/G05-live-diverges-from-main.md)) | `node scripts/deploy-check.mjs` · `gh run list --workflow deploy-smoke --limit 3` |

## Πώς μένει αληθινό

Κανένας βαθμός δεν ανεβαίνει επειδή «έγινε δουλειά» — μόνο όταν η συνθήκη της γραμμής
εκπληρωθεί και ΞΑΝΑΜΕΤΡΗΘΕΙ. Κανένας αριθμός χωρίς την εντολή που τον παράγει ή ημερομηνία.
Η κατάσταση των κλάσεων αστοχίας δεν γράφεται εδώ — παράγεται:
`node scripts/catalogue-status.mjs` → [failure-catalogue/STATUS.md](failure-catalogue/STATUS.md).
Το γνωστό χρέος ζει ΜΟΝΟ στο [known-debt.json](known-debt.json): κάθε εγγραφή με λόγο και λήξη,
επιτρέπεται να μείνει, απαγορεύεται να μεγαλώσει, και πέφτει μόνη της όταν λήξει ή όταν πάψει να
καλύπτει κάτι (ΝΕΚΡΗ).
