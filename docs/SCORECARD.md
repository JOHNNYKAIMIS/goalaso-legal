# Goalaso Legal — Βαθμολογία ποιότητας (SOURCE OF TRUTH)

> Πρώτη μέτρηση: 2026-08-21, HEAD `30421cc`. Επαλήθευση 2026-08-22 σε clone, ίδιο HEAD — όλες οι
> γραμμές ξαναμετρήθηκαν· ό,τι διαφέρει από την πρώτη μέτρηση φέρει ημερομηνία.
> Κάθε γραμμή έχει anchor που ΤΡΕΞΕ — όχι εντύπωση. Βαθμός ανεβαίνει ΜΟΝΟ όταν ξαναμετρηθεί.

## Τι είναι αυτό το repo (χαρτογράφηση 2026-08-21)

Τρεις στατικές σελίδες HTML — `index`, `privacy`, `terms` (`wc -l *.html` → μετρήθηκε 192 γραμμές
συνολικά στις 2026-08-21) — οι νομικές σελίδες της εφαρμογής Goalaso (οργάνωση ερασιτεχνικού
futsal). Δίγλωσσες (ελληνικά + αγγλικά στο ίδιο αρχείο). Κανένα build, κανένα test, κανένα CI
(`ls -A .github 2>/dev/null` → τίποτα). Ό,τι γίνεται push βγαίνει όπως είναι.

**Πώς βγαίνει (μετρήθηκε 2026-08-22):** GitHub Pages με source `main` `/`, legacy build
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/pages --jq '.source,.build_type'`). Το ΜΟΝΟ που τρέχει
σε push είναι το dynamic `pages-build-deployment`
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/actions/workflows --jq '.workflows[].path'` → μόνο
`dynamic/pages/pages-build-deployment`) — deploy, όχι έλεγχος. Δηλαδή **push στο main =
δημοσίευση μέσα σε δευτερόλεπτα** (push 16:23:26Z → run 16:23:31Z), χωρίς branch protection
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/branches/main/protection` → 404). Το
`goalaso.net/privacy.html` ΔΕΝ είναι αυτό το repo: επιστρέφει το SPA της εφαρμογής
(`curl -s https://goalaso.net/privacy.html | grep -o '<title>[^<]*'` → «Goalaso — Όλα για την
μπάλα.»). Ο σύνδεσμος που βλέπουν χρήστες και stores είναι το github.io (app repo:
`grep -rn goalaso-legal apps/mobile/src/utils/support.ts docs/marketing/play-console-paste-sheet.md`).

**Ποιος τις διαβάζει:** οι χρήστες της εφαρμογής (link από τα stores/app) και —το σημαντικότερο—
όποιος ασκήσει δικαίωμα GDPR ή υποβάλει καταγγελία στην Επίτροπο Κύπρου.

**Το χειρότερο πιθανό σφάλμα:** η πολιτική να υπόσχεται κάτι που η εφαρμογή δεν κάνει (ή το
αντίστροφο). Δεν είναι υπόθεση — συνέβη: το ιστορικό (`git log --oneline`) δείχνει ότι η
πολιτική περιέγραφε διαγραφή «μόνο μέσω email» ενώ η εφαρμογή απέκτησε in-app διαγραφή, και
διορθώθηκε με το χέρι όταν το πρόσεξε κάποιος (commit `30421cc`, 2026-08-03).

**Τι το εμποδίζει σήμερα:** τίποτα. Κανένας έλεγχος δεν τρέχει πουθενά.

## Η κλίμακα

| | |
|---|---|
| ≤4 | Το πρόβλημα υπάρχει και κανείς δεν το βλέπει |
| 5-6 | Το ξέρουμε, δεν το εμποδίζει τίποτα |
| 7-8 | Υπάρχει μηχανισμός, αλλά δεν έχει αποδειχθεί ότι πιάνει |
| 9 | Μηχανισμός που έχει αποδειχθεί ότι πέφτει όταν σπάσει αυτό που φυλάει |
| 10 | Το χρέος δεν υπάρχει πια |

## Α. Ακρίβεια περιεχομένου — (5+5+5)/3 = 5,0/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Συμφωνία πολιτικής ↔ εφαρμογής | **5** | 8 | Οι ισχυρισμοί («in-app διαγραφή», «token hashed», «Φρανκφούρτη», «καμία διαφήμιση») ζουν ΜΟΝΟ σε πεζογραφία· η εφαρμογή ζει αλλού και τίποτα δεν τα δένει. Η απόκλιση ΕΧΕΙ συμβεί (commit `30421cc`). **Για 7-8:** κατάλογος «ελέγξιμων ισχυρισμών» ανά σελίδα + ένα checklist που τρέχει σε κάθε release της εφαρμογής (κλάση [G01](failure-catalogue/G01-policy-lags-app.md)) | `git log --format='%ad %s' --date=short` |
| Ημερομηνία «Τελευταία ενημέρωση» αληθής | **5** | 9 | Μετρήθηκε 2026-08-21: το privacy.html γράφει «4 Αυγούστου 2026», αλλά το commit που το έγραψε έγινε **3 Αυγούστου 19:23** (`git log --date=iso -S"4 Αυγούστου"`· επαληθεύτηκε 2026-08-22 σε clone: `30421cc 2026-08-03 19:23:26 +0300`, τα 3 προηγούμενα commits έγραφαν «3 Αυγούστου»). Η ΜΟΝΗ υπόσχεση αλλαγών προς τον χρήστη («θα ενημερωθεί η ημερομηνία») συντηρείται με το χέρι και ήταν ήδη λάθος από την πρώτη μέρα. Ήταν 4 (κανείς δεν το είχε δει) → 5 με την καταγραφή. **Για 9:** η ημερομηνία να παράγεται/ελέγχεται από το git ([G03](failure-catalogue/G03-hand-maintained-date.md)) | `privacy.html:20` vs `git show -s --date=iso HEAD` |
| Στοιχεία επικοινωνίας ενεργά | **5** | 8 | Το κείμενο δηλώνει το gmail «προσωρινό — θα αλλάξει σε privacy@goalaso.net μόλις ενεργοποιηθεί το routing» (μετρήθηκε: `grep -n "privacy@goalaso.net" privacy.html` → 1 εμφάνιση, μόνο στα ελληνικά). Εκκρεμής απόφαση ΧΩΡΙΣ ημερομηνία λήξης. **ΔΕΝ ΜΕΤΡΗΘΗΚΕ:** αν το mailbox απαντά — θέλει δοκιμαστικό email από άνθρωπο. **Για 8:** ή ενεργοποίηση του routing ή αφαίρεση της υπόσχεσης | `privacy.html:26` |

## Β. Δίγλωσση συνέπεια — 5/1 = 5,0/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Ισοδυναμία EL ↔ EN | **5** | 9 | Δομικά ΟΚ (μετρήθηκε 2026-08-21: terms 8/8 ενότητες h2, privacy 4/4 bullets, ημερομηνίες ταυτίζονται) ΑΛΛΑ βρέθηκε απόκλιση περιεχομένου: η σημείωση «προσωρινό email» υπάρχει ΜΟΝΟ στα ελληνικά — ο αγγλόφωνος χρήστης διαβάζει άλλη πολιτική επικοινωνίας. Επιπλέον (μετρήθηκε 2026-08-22): `grep -c 'lang="en"' *.html` → 0 και στα 3 αρχεία — μόνο `<html lang="el">`, το αγγλικό μισό δηλώνεται ελληνικό (screen readers / αυτόματη μετάφραση το διαβάζουν ως ελληνικά). Δύο χειρόγραφα αντίγραφα, κανένας μηχανισμός. **Για 7-8:** φύλακας δομικής ισότητας (πλήθος h2/li/ημερομηνίες EL=EN, EN τμήμα μέσα σε `lang="en"`) στο CI ([G02](failure-catalogue/G02-bilingual-divergence.md)) | `grep -c "<h2>" terms.html` · `privacy.html:26` · `grep -c 'lang="en"' *.html` |

## Γ. Μηχανική παράδοσης — (2+7)/2 = 4,5/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Έλεγχοι σε κάθε αλλαγή | **2** | 9 | ΔΕΝ ΥΠΑΡΧΕΙ ΤΙΠΟΤΑ: κανένα workflow στο repo, κανένα script (μετρήθηκε: `ls -A .github docs scripts 2>/dev/null`). Το μόνο workflow που τρέχει είναι το dynamic Pages deploy (μετρήθηκε 2026-08-22: 4/4 runs success) — δεν ελέγχει τίποτα, απλώς δημοσιεύει. Σπασμένο HTML, mojibake, νεκρό link — όλα βγαίνουν σιωπηλά. **Για 7:** ένα workflow με (α) HTML validity, (β) έλεγχο εσωτερικών links, (γ) τον φύλακα EL/EN ([G04](failure-catalogue/G04-nothing-runs-on-push.md)) | `ls -A .github 2>/dev/null` · `gh api repos/JOHNNYKAIMIS/goalaso-legal/actions/workflows --jq '.workflows[].path'` |
| Το ζωντανό deploy σερβίρει το HEAD | **7** | 9 | Μετρήθηκε 2026-08-22 (στις 2026-08-21 το egress προς `johnnykaimis.github.io` / `goalaso.net` ήταν μπλοκαρισμένο): `curl` τα 3 αρχεία από `https://johnnykaimis.github.io/goalaso-legal/` + `diff` με το HEAD → **3/3 IDENTICAL**· τελευταίο run `pages build and deployment` @ `30421cc` success. Μηχανισμός υπάρχει (αυτόματο deploy από main), τίποτα δεν το ελέγχει μετά — πρώτη μέτρηση, όχι άνοδος. **Παγίδα:** `gh api repos/JOHNNYKAIMIS/goalaso-legal/pages/builds/latest --jq .commit` δείχνει `9bfa4b6` (το ΠΡΟΗΓΟΥΜΕΝΟ commit) ενώ το περιεχόμενο είναι του `30421cc` — μέτρα με diff περιεχομένου, ποτέ με αυτό το πεδίο. **Για 9:** post-deploy smoke που κάνει diff live ↔ HEAD και κοκκινίζει ([G04](failure-catalogue/G04-nothing-runs-on-push.md)) | `for f in index privacy terms; do curl -s https://johnnykaimis.github.io/goalaso-legal/$f.html \| diff -q - $f.html; done` |

## Πώς μένει αληθινό

Κανένας βαθμός δεν ανεβαίνει επειδή «έγινε δουλειά» — μόνο όταν η συνθήκη της γραμμής
εκπληρωθεί και ΞΑΝΑΜΕΤΡΗΘΕΙ. Κανένας αριθμός χωρίς την εντολή που τον παράγει ή ημερομηνία.
