# Goalaso Legal — Βαθμολογία ποιότητας (SOURCE OF TRUTH)

> Πρώτη μέτρηση: 2026-08-21, HEAD `30421cc`. Επαλήθευση 2026-08-22 σε clone, ίδιο HEAD — όλες οι
> γραμμές ξαναμετρήθηκαν· ό,τι διαφέρει από την πρώτη μέτρηση φέρει ημερομηνία.
> Γύρος 1: 2026-08-22 — κλάση G04 απέκτησε φύλακα με απόδειξη (branch `g04-quality-workflow`).
> Γύρος 2: 2026-08-22 — κλάσεις G02 + G03 απέκτησαν φύλακες με απόδειξη (branch `g02-g03-guards`)·
> το σημερινό χρέος κλειδώθηκε στο [docs/known-debt.json](known-debt.json) με λόγο και λήξη 2026-09-21.
> Γύρος 3: 2026-08-22 — νέα κλάση G05 (το live αποκλίνει από το main) με φύλακα και απόδειξη (branch `g05-deploy-smoke`).
> Γύρος 4: 2026-08-22 — G01 μετρήθηκε στον κώδικα της εφαρμογής (`goalaso` @ `ad56785`): το περιστατικό ΞΑΝΑΣΥΝΕΒΗ
> (όνομα/επώνυμο από 18/8) + 3 κενά· κατάλογος `docs/claims.json` + φύλακας `claims:check` με απόδειξη (branch `g01-claims`).
> Γύρος 5: 2026-08-22 — G03 author date αντί committer (PR #6)· PR #7: η πολιτική διορθωμένη (4 ευρήματα + ημερομηνία
> 22/8 + `lang="en"` + EN «προσωρινό»), γνωστό χρέος 7 → 1 εγγραφή. Γύρος 6: README (PR #8).
> **Γύρος 7: 2026-08-22 — MERGES (εντολή ιδιοκτήτη): #8 → `daf1463`, #7 → `288b995`, όλα με merge commit. Ξαναμέτρηση
> στο main @ `288b995`: 11/11 φύλακες + αποδείξεις πράσινα, `deploy-smoke` (dispatch) success, live = main byte-προς-byte.**
> Κάθε γραμμή έχει anchor που ΤΡΕΞΕ — όχι εντύπωση. Βαθμός ανεβαίνει ΜΟΝΟ όταν ξαναμετρηθεί στο SHA που μπήκε στο main.

## Τι είναι αυτό το repo (χαρτογράφηση 2026-08-21)

Τρεις στατικές σελίδες HTML — `index`, `privacy`, `terms` (`wc -l *.html` → μετρήθηκε 192 γραμμές
συνολικά στις 2026-08-21) — οι νομικές σελίδες της εφαρμογής Goalaso (οργάνωση ερασιτεχνικού
futsal). Δίγλωσσες (ελληνικά + αγγλικά στο ίδιο αρχείο). Μέχρι τον γύρο 1: κανένα build, κανένα
test, κανένα CI (`ls -A .github 2>/dev/null` → τίποτα στις 2026-08-21). Ό,τι γίνεται push βγαίνει
όπως είναι. Από τον γύρο 7 (στο main): `.github/workflows/quality.yml` σε κάθε push + PR,
`deploy-smoke.yml` καθημερινά, `npm run check` τοπικά, χωρίς εξαρτήσεις (`cat package.json` → `scripts` μόνο).
Σημείο εισόδου: [README.md](../README.md).

**Πώς βγαίνει (μετρήθηκε 2026-08-22):** GitHub Pages με source `main` `/`, legacy build
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/pages --jq '.source,.build_type'`). Μέχρι τον γύρο 1 το
ΜΟΝΟ που έτρεχε σε push ήταν το dynamic `pages-build-deployment`
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/actions/workflows --jq '.workflows[].path'` → μόνο
`dynamic/pages/pages-build-deployment`) — deploy, όχι έλεγχος. Δηλαδή **push στο main =
δημοσίευση μέσα σε δευτερόλεπτα** (push 16:23:26Z → run 16:23:31Z), χωρίς branch protection
(`gh api repos/JOHNNYKAIMIS/goalaso-legal/branches/main/protection` → 404, ξανά 2026-08-22). Το
`goalaso.net/privacy.html` ΔΕΝ είναι αυτό το repo: επιστρέφει το SPA της εφαρμογής
(`curl -s https://goalaso.net/privacy.html | grep -o '<title>[^<]*'` → «Goalaso — Όλα για την
μπάλα.»). Ο σύνδεσμος που βλέπουν χρήστες και stores είναι το github.io (app repo:
`grep -rn goalaso-legal apps/mobile/src/utils/support.ts docs/marketing/play-console-paste-sheet.md`).
Μετρήθηκε στον γύρο 7: το merge στο main με αλλαγή HTML (`288b995`, 18:22:34Z) είχε πράσινο
`deploy:check` στις 18:23:16Z — 42 δευτερόλεπτα, μαζί με το Pages deploy.

**Ποιος τις διαβάζει:** οι χρήστες της εφαρμογής (link από τα stores/app) και —το σημαντικότερο—
όποιος ασκήσει δικαίωμα GDPR ή υποβάλει καταγγελία στην Επίτροπο Κύπρου.

**Το χειρότερο πιθανό σφάλμα:** η πολιτική να υπόσχεται κάτι που η εφαρμογή δεν κάνει (ή το
αντίστροφο). Δεν είναι υπόθεση — συνέβη: το ιστορικό (`git log --oneline`) δείχνει ότι η
πολιτική περιέγραφε διαγραφή «μόνο μέσω email» ενώ η εφαρμογή απέκτησε in-app διαγραφή, και
διορθώθηκε με το χέρι όταν το πρόσεξε κάποιος (commit `30421cc`, 2026-08-03). Και ξανασυνέβη
2026-08-18 (όνομα/επώνυμο) — μετρήθηκε 2026-08-22, διορθώθηκε στο main 2026-08-22 (`288b995`).

**Τι το εμποδίζει σήμερα (main @ `288b995`):** πέντε φύλακες με απόδειξη σε κάθε push/PR — G04
(δομή HTML, εσωτερικά links, κωδικοποίηση), G02 (δομή/ημερομηνία/γλώσσα EL = EN), G03 (ημερομηνία
σελίδας = author date του τελευταίου ουσιώδους commit), G05 (το live σερβίρει byte-προς-byte το
`origin/main`, χωρίς CDN cache, + καθημερινά), G01 (ο κατάλογος [claims.json](claims.json) δένει
κάθε ισχυρισμό με το κείμενο και με μέτρηση στον κώδικα της εφαρμογής — εδώ χωρίς `--app`, στο CI
της εφαρμογής ΟΧΙ ακόμα: [app-release-checklist.md](app-release-checklist.md)).
`node scripts/catalogue-status.mjs` → [STATUS.md](failure-catalogue/STATUS.md): 5/5 ΦΥΛΑΣΣΟΝΤΑΙ.
Γνωστό χρέος: **1 εγγραφή** (C16 — το goalaso.net συλλέγει email χωρίς σύνδεσμο προς την πολιτική,
διορθώνεται μόνο στο repo της εφαρμογής, λήγει 2026-09-21).

## Η κλίμακα

| | |
|---|---|
| ≤4 | Το πρόβλημα υπάρχει και κανείς δεν το βλέπει |
| 5-6 | Το ξέρουμε, δεν το εμποδίζει τίποτα |
| 7-8 | Υπάρχει μηχανισμός, αλλά δεν έχει αποδειχθεί ότι πιάνει |
| 9 | Μηχανισμός που έχει αποδειχθεί ότι πέφτει όταν σπάσει αυτό που φυλάει |
| 10 | Το χρέος δεν υπάρχει πια |

## Α. Ακρίβεια περιεχομένου — (6+9+5)/3 = 6,7/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Συμφωνία πολιτικής ↔ εφαρμογής | **6** | 8 | Ήταν 5 (πεζογραφία χωρίς δεσμό· η απόκλιση συνέβη στο `30421cc`). **Γύρος 4:** μετρήθηκε στον κώδικα της εφαρμογής — ΞΑΝΑΣΥΝΕΒΗ (όνομα/επώνυμο 18/8) + 3 κενά (crash reports με `auth_id`/`route`, λίστα αναμονής με email, Render/Expo). Ό,τι η πολιτική λέει αρνητικά ισχύει (0 SDK κάμερας/GPS/πληρωμών/διαφημίσεων). Κατάλογος [claims.json](claims.json) + φύλακας `claims:check` (anchors ↔ κείμενο, χρέος με λήξη, μέτρηση ≤ 60 ημερών) + `--app` ξαναμέτρηση· απόδειξη 13/13 κόκκινες, 3/3 αθώες, fail-closed 4/4. **Γύρος 7 (main @ `288b995`):** η πολιτική ενημερωμένη για τα 4 ευρήματα — 21 ισχυρισμοί, 18 ΕΠΙΒΕΒΑΙΩΜΕΝΟΙ, 1 ΚΕΝΟ (C16), 2 χειροκίνητοι· `--app` 19/19 @ `c169bd7`. Μένει **6**: ο μηχανισμός που πιάνει την ΑΛΛΑΓΗ της εφαρμογής ζει μόνο ως `--app` (τοπικά / release checklist), όχι στο CI της εφαρμογής. **Για 7:** το προτεινόμενο workflow `privacy-claims` ([app-release-checklist.md](app-release-checklist.md)) τρέχει πράσινο στο repo της εφαρμογής + σύνδεσμος πολιτικής στο goalaso.net (C16). **Για 8:** αποδεδειγμένο ότι κοκκινίζει σε PR της εφαρμογής ([G01](failure-catalogue/G01-policy-lags-app.md)) | `node scripts/claims-check.mjs --app ../goalaso` |
| Ημερομηνία «Τελευταία ενημέρωση» αληθής | **9** | 9 | Ήταν 4 → 5 (2026-08-21: «4 Αυγούστου» ενώ το commit ήταν 3/8 19:23 +0300) → 7 (γύρος 2: φύλακας `date:check` με απόδειξη, το λάθος κλειδωμένο ως χρέος). **Γύρος 5-7:** `date:check` συγκρίνει author date (`%as` — rebase/merge commit την κρατούν, squash όχι· απόδειξη 12/12 κόκκινες, 4/4 αθώες, fail-closed 3/3). Η ουσιώδης ενημέρωση `815a16b` γράφει «22 Αυγούστου 2026» = author date· το `[no-date]` `864fb7a` αγνοείται. **Ξαναμετρήθηκε στο main @ `288b995`:** `date:check` 2/2 σελίδες = τελευταίο ουσιώδες commit, 0 χρέος ημερομηνίας, CI στο main πράσινο (run `32590547586`). 7 → **9**. Η σύμβαση: ουσιώδης αλλαγή = νέα ημερομηνία, μη ουσιώδης = `[no-date]`, **merge χωρίς squash** (squash ακόμα ενεργό στις ρυθμίσεις — ρύθμιση ιδιοκτήτη) ([G03](failure-catalogue/G03-hand-maintained-date.md)) | `node scripts/date-check.mjs` · `git log --format='%h %as %s' -- privacy.html` |
| Στοιχεία επικοινωνίας ενεργά | **5** | 8 | Το κείμενο δηλώνει το gmail «προσωρινό — θα αλλάξει σε privacy@goalaso.net μόλις ενεργοποιηθεί το routing» — από τον γύρο 7 και στις δύο γλώσσες (`grep -c privacy@goalaso.net privacy.html` → 2). Εκκρεμής απόφαση ΧΩΡΙΣ ημερομηνία λήξης. **ΔΕΝ ΜΕΤΡΗΘΗΚΕ:** αν το mailbox απαντά — θέλει δοκιμαστικό email από άνθρωπο. **Για 8:** ή ενεργοποίηση του routing ή αφαίρεση της υπόσχεσης (απόφαση ιδιοκτήτη) | `grep -n "privacy@goalaso.net" privacy.html` |

## Β. Δίγλωσση συνέπεια — 9/1 = 9,0/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Ισοδυναμία EL ↔ EN | **9** | 9 | Ήταν 5 (2026-08-21: η σημείωση «προσωρινό» μόνο στα ελληνικά· 2026-08-22: `lang="en"` 0/2 δίγλωσσες σελίδες) → 7 (γύρος 2: φύλακας `bilingual:check` — h2/ul/li/a/p EL = EN με εξαίρεση το EL-only `lang-note`, ημερομηνία EL = EN, αγγλικό τμήμα σε `lang="en"`· απόδειξη 12/12 κόκκινες, 3/3 αθώες, fail-closed 2/2· το χρέος κλειδωμένο). **Γύρος 5-7:** `<section lang="en">` και στις 2 σελίδες (`[no-date]` `864fb7a`), η σημείωση «προσωρινό» και στα αγγλικά, 0 χρέος bilingual. **Ξαναμετρήθηκε στο main @ `288b995`:** 2 δίγλωσσες σελίδες, h2 7=7/8=8 · li 11=11/0=0 · a 3=3/1=1, ημερομηνίες EL=EN 2/2, `lang="en"` 2/2, CI στο main πράσινο. 7 → **9**. Ρητά εκτός: η σημασιολογική απόκλιση (πρόταση που λέει άλλο πράγμα) δεν πιάνεται από φύλακα — μόνο άνθρωπος, σε κάθε PR ([G02](failure-catalogue/G02-bilingual-divergence.md)) | `node scripts/bilingual-check.mjs` · `grep -c 'lang="en"' privacy.html terms.html` |

## Γ. Μηχανική παράδοσης — (9+9)/2 = 9,0/10

| Διάσταση | Τωρινό | Στόχος | Μέτρηση & επόμενο βήμα | Anchor |
|---|---|---|---|---|
| Έλεγχοι σε κάθε αλλαγή | **9** | 9 | Ήταν 2 (2026-08-21: κανένα workflow, κανένα script· το push του `cc73d1f` πυροδότησε 0 runs). **Γύρος 1:** `quality.yml` σε push + PR με `html:check` (α)(β)(γ) + απόδειξη + `catalogue:check`· η απόδειξη έπιασε false positive πριν το CI. **Γύροι 2-4:** (δ) G02 + G03, G05, G01 μέσα στο ίδιο workflow, όλοι με αποδείξεις· το STATUS.md παράγεται και ελέγχεται (`--check`). **Γύρος 7 — ξαναμετρήθηκε στο main:** push `daf1463` (εργαλεία + README) → run `32590474218` success· push `288b995` (αλλαγή HTML) → run `32590547586` success, 12/12 βήματα· `npm run check` τοπικά στο main: 11/11 πράσινα. 7 → **9**: μηχανισμός στο trunk, αποδεδειγμένος (5 αποδείξεις: 13+12+12+4+13 μεταλλάξεις κόκκινες) ([G04](failure-catalogue/G04-nothing-runs-on-push.md)). Όχι 10: χωρίς branch protection ένα απευθείας push στο main παρακάμπτει τα πάντα (απόφαση ιδιοκτήτη) | `gh run list --workflow quality --branch main --limit 3` · `npm run check` |
| Το ζωντανό deploy σερβίρει το HEAD | **9** | 9 | Ήταν αμέτρητο → 7 (2026-08-22: 3/3 byte-identical, μηχανισμός χωρίς έλεγχο· παγίδες: `pages/builds/latest .commit` δείχνει το προηγούμενο commit, `Cache-Control: max-age=600`) → 8 (γύρος 3: `deploy:check` live ↔ `origin/main` χωρίς CDN cache, με αναμονή· απόδειξη 4/4 κόκκινες, 4/4 αθώες, fail-closed 3/3· `deploy-smoke.yml` καθημερινά). **Γύρος 7 — πραγματικό deploy:** merge `288b995` στις 18:22:34Z με αλλαγή HTML → `deploy:check` στο CI του main πράσινο 18:23:16Z (42 s, περίμενε το Pages deploy)· `deploy-smoke` (workflow_dispatch) run `32590646067` success· τοπικά `deploy:check` 3/3 @ `288b995`· το live δείχνει «22 Αυγούστου 2026» + `<section lang="en">` (`curl … ?nocache=`). 8 → **9**. ΔΕΝ ΜΕΤΡΗΘΗΚΕ ακόμα: το cron (06:17 UTC, πρώτη φορά αύριο) — ίδιο job, άλλο trigger ([G05](failure-catalogue/G05-live-diverges-from-main.md)) | `node scripts/deploy-check.mjs` · `gh run list --workflow deploy-smoke --limit 3` |

## Πώς μένει αληθινό

Κανένας βαθμός δεν ανεβαίνει επειδή «έγινε δουλειά» — μόνο όταν η συνθήκη της γραμμής
εκπληρωθεί και ΞΑΝΑΜΕΤΡΗΘΕΙ στο SHA που μπήκε στο main. Κανένας αριθμός χωρίς την εντολή που τον
παράγει ή ημερομηνία. Η κατάσταση των κλάσεων αστοχίας δεν γράφεται εδώ — παράγεται:
`node scripts/catalogue-status.mjs` → [failure-catalogue/STATUS.md](failure-catalogue/STATUS.md).
Το γνωστό χρέος ζει ΜΟΝΟ στο [known-debt.json](known-debt.json): κάθε εγγραφή με λόγο και λήξη,
επιτρέπεται να μείνει, απαγορεύεται να μεγαλώσει, και πέφτει μόνη της όταν λήξει ή όταν πάψει να
καλύπτει κάτι (ΝΕΚΡΗ). Οι συμβάσεις για όποιον αγγίζει σελίδα: [README.md](../README.md).
